version: '3.8'

services:
  runic-converter:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - ./app:/app
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    restart: unless-stopped

