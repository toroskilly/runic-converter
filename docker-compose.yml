version: '3.8'

services:
  runic-converter:
    image: python:3.9-slim
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=app.py
    working_dir: /app
    command: >
      sh -c "
      apt-get update && apt-get install -y git &&
      git clone https://github.com/toroskilly/runic-converter.git /app &&
      cd /app &&
      pip install --no-cache-dir -r requirements.txt &&
      python app.py
      "
    restart: unless-stopped
